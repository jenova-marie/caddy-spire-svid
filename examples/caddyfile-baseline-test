# 🌸 Baseline Caddy test - NO SPIRE integration
# Just basic HTTP/HTTPS with self-signed certs

# HTTPS test site with automatic certificates
localhost:8443 {
    respond "🌸 HTTPS Caddy baseline working! Self-signed cert ✨"
}

# HTTP test site  
localhost:8080 {
    respond "🌸 HTTP Caddy baseline working! No SPIRE needed ✨"
}

# Admin API
localhost:2019 {
    respond "🌸 Caddy Admin API baseline working! ✨"
}
